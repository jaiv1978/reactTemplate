import { makeStyles } from '@material-ui/styles';

const MainStyle = makeStyles({
    root : {
        minHeight : "100vh",
        padding: "0px",
        margin: "0px"
    }
})

export default MainStyle;